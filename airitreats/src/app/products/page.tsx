
import { createClient } from "@/prismicio";


export default async function page() {
    const client = createClient();

    // let data = await client.getAllByEveryTag(["product", "product"])
    // let singleData = await client.getByUID("product", "1");


    return (
        <>

        </>
    )
}
